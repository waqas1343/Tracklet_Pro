import 'package:flutter/material.dart';

class DistributorDriverScreen extends StatelessWidget {
  const DistributorDriverScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}