import 'package:flutter/material.dart';

class DistributorSettings extends StatelessWidget {
  const DistributorSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}